---
description: Full repo audit — monorepo-aware, per-package + cross-package
argument-hint: "<repo path, single package name, or empty for cwd>"
---

# /audit

Wraps `repo-audit`. Output to `docs/audit/audit.{md,json}` by default.

```
/audit
/audit packages/api               # audit one package only
```
