---
description: Generate or update README, docs/, ADR stubs from the audit JSON
argument-hint: "<artifact set: 'all', 'readme', 'architecture', 'runbook', 'adrs', 'per-package'>"
---

# /docs

Wraps `docs-author`.

```
/docs all
/docs readme
/docs adrs
```
