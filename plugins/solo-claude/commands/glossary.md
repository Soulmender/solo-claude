---
description: Extract the domain glossary (ubiquitous language) from the code
argument-hint: "<empty for full repo, or a package to scope>"
---

# /glossary

Wraps `domain-glossary`. Writes `docs/glossary.md` + `glossary.json` — domain terms defined from usage, plus synonym and collision flags.

```
/glossary
/glossary packages/api
```
